on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
       
      - name: 'login to Docker hub'
        run: docker login --username ${{ secrets.DOCKERUSERNAME }} --password-stdin ${{ secrets.DOCKERPASSWORD }} 
